# Coffee System

Простой веб-проект для управления продажей кофе и сопутствующих товаров.  
Разработан на **Python + Flask + SQLite** с HTML/CSS фронтендом.  

Проект предназначен для демонстрационных задач по компетенции "Веб-технологии".  

---

## 🚀 Возможности проекта

- Авторизация пользователей с ролями:
  - **Администратор** – полный доступ
  - **Менеджеры** – ограниченный доступ
- CRUD операции:
  - Товары
  - Клиенты
  - Заказы
- Импорт данных из CSV файлов:
  - `csv/товары.csv`
  - `csv/клиенты.csv`
- Автоматическая инициализация базы данных и создание пользователей при первом запуске
- Простой адаптивный фронтенд на HTML/CSS
- Валидация полей и вывод ошибок на форме

---

## 👤 Доступные пользователи

| Роль | Логин      | Пароль         |
|------|------------|----------------|
| Admin | admin     | kofeman        |
| Manager | manager1 | manager2026   |
| Manager | manager2 | manager262    |

---

## 📂 Структура проекта

```
coffee_system/
│
├─ app.py # основной Flask сервер
├─ database.db # база данных SQLite (создаётся автоматически)
├─ csv/ # CSV файлы для импорта данных
│ ├─ товары.csv
│ └─ клиенты.csv
├─ templates/ # HTML-шаблоны
│ ├─ login.html
│ ├─ dashboard.html
│ ├─ users.html
│ ├─ products.html
│ ├─ clients.html
│ └─ orders.html
└─ static/
└─ style.css # CSS стили
```

---

## ⚙ Установка и запуск

1. Убедитесь, что у вас установлен **Python 3.10+**.  
   Скачать: [https://www.python.org/downloads/](https://www.python.org/downloads/)

2. Установите Flask и зависимости:

```powershell
python -m pip install --upgrade pip
pip install -r requirements.txt
```
Если Python не видит команды pip, убедитесь, что путь к скриптам добавлен в PATH или используйте python -m pip install ....

3. Запустите сервер:
```
python app.py
```

3. Откройте браузер и перейдите по адресу:

    http://127.0.0.1:5000


4. Войдите с учётными данными (см. таблицу выше).

## 📝 Примечания

База данных создаётся автоматически при первом запуске (database.db).

CSV файлы для импорта должны быть в папке csv/.

Все изменения (товары, клиенты, заказы) сохраняются в database.db.

Для адаптации фронтенда используется простой CSS, можно расширять под планшеты.

Создано для учебных целей и демонстрации базовой работы с Flask, HTML/CSS и базой данных SQLite.